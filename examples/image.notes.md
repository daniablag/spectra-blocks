# Notes for image block

This block registers in `includes/blocks/image/block.php`. Attributes live in `includes/blocks/image/attributes.php` and styles load from `includes/blocks/image/frontend.css.php`.
